# Claude Code 작업 인수인계 프롬프트

## 🎯 작업 목표

하이마트 호남지사의 **재고 현황 대시보드**를 유지보수하고 지속적으로 개선하는 것

현재 상태: ✅ **v1.0 완료 및 웹 배포 중**

---

## 📦 현재 프로젝트 상태

### 완성된 산출물

1. **호남지사_재고대시보드.html** (0.8MB)
   - 완전한 웹 애플리케이션 (독립형)
   - 4,980건의 호남지사 재고 데이터 포함
   - Chart.js로 통계 시각화

2. **Artifact URL**
   - 라이브 배포됨 (온라인 공유 가능)
   - 추가 JSON 파일 필요 없음 (데이터 임베드됨)

3. **원본 데이터 파일**
   - `판매재고현황_20260915.xlsx` (72,567건 전체)
   - `소진모델.xlsx` (61개 모델 상태)
   - `하이마트 지점 index.xlsx` (지점 정보)

---

## 🔄 향후 작업 예상 항목

### 긴급 우선순위 (P0)

1. **월별 데이터 업데이트**
   ```
   매월 새로운 판매재고현황_{YYYYMMDD}.xlsx 받음
   → Python 스크립트로 처리
   → HTML에 임베드
   → Artifact 재배포
   ```

2. **긴급 버그 수정**
   - 데이터 누락 시 복구
   - 필터링 오류 수정
   - 검색 기능 개선

### 보통 우선순위 (P1)

1. **기능 개선**
   - [ ] 모바일 반응형 최적화
   - [ ] 엑셀/CSV 다운로드 기능
   - [ ] 인쇄 기능 추가
   - [ ] 다크모드 지원

2. **성능 최적화**
   - [ ] 대용량 데이터 처리 (가상 스크롤)
   - [ ] 로딩 속도 개선
   - [ ] 메모리 최적화

3. **분석 기능 추가**
   - [ ] 시간별/주별 판매 추이
   - [ ] 회전율 분석
   - [ ] 재고 부족 알림

### 장기 우선순위 (P2)

1. **아키텍처 개선**
   - [ ] React/Vue로 마이그레이션
   - [ ] 데이터베이스 연동
   - [ ] API 개발

2. **확장 기능**
   - [ ] 다른 팀/채널 지원
   - [ ] 사용자 권한 관리
   - [ ] 데이터 버전 관리

---

## 🛠️ 유지보수 체크리스트

### 월간 (Monthly)
- [ ] 새 재고 데이터 수집
- [ ] 데이터 처리 및 검증
- [ ] HTML 업데이트
- [ ] Artifact 재배포
- [ ] 성능 모니터링

### 분기별 (Quarterly)
- [ ] 사용자 피드백 수집
- [ ] 기능 개선 우선순위 검토
- [ ] 성능 벤치마크
- [ ] 호환성 테스트

### 연간 (Yearly)
- [ ] 아키텍처 검토
- [ ] 기술 스택 업그레이드
- [ ] 대규모 리팩토링 계획

---

## 📋 데이터 처리 워크플로우

### 입력
```
판매재고현황_{날짜}.xlsx (72,567건)
  ├─ 모든 지사 데이터
  ├─ 모든 상품 코드
  └─ 당월 판매/회전율 정보

소진모델.xlsx (61개)
  └─ 모델코드 → 진열상태 매핑

하이마트 지점 index.xlsx
  └─ 지점명 참고 정보
```

### 처리 로직
```python
# 1. 호남지사만 필터링
df_honam = df_stock[df_stock['지사명'] == '호남지사']  # 7,240건

# 2. 재고 0 제외
df_honam = df_honam[df_honam['잔여재고'] > 0]  # 4,980건

# 3. 제품군 분류 (상품명 기반)
def extract_category(name):
    if 'TV' in name: return 'TV'
    elif '냉장고' in name: return '냉장고'
    # ... (10개 카테고리)

# 4. 진열상태 매핑
def check_status(model_code):
    return dispose_models.get(model_code, '진열대상')

# 5. JSON 생성 및 HTML 임베드
data = {
    '전체데이터': [...],
    '모델별현황': {...},
    '통계': {...}
}
```

### 출력
```
호남지사_재고대시보드.html (완전한 웹 앱)
  ├─ 4,980건 재고 데이터
  ├─ 모든 필터/검색 로직
  └─ 차트 렌더링
```

---

## 🔍 코드 구조

### 프론트엔드 아키텍처

```
호남지사_재고대시보드.html
├─ 헤더
│  └─ 제목 + 설명
├─ 통계 대시보드
│  ├─ 전체 재고품목
│  ├─ 진열대상/소진대상
│  ├─ 제품군 수
│  └─ 지점 수
├─ 필터/검색 패널
│  ├─ 텍스트 검색
│  ├─ 제품군 드롭다운
│  ├─ 진열상태 드롭다운
│  ├─ 지점명 드롭다운
│  └─ 검색/초기화 버튼
├─ 탭 (재고현황 / 분석)
│  ├─ 테이블 (정렬 가능)
│  └─ 차트 (Chart.js)
└─ 모달 (모델별 지점 현황)
```

### 핵심 JavaScript 함수

```javascript
initDashboard()           // 전체 초기화
initializeFilters()       // 필터 옵션 생성
applyFilters()           // 필터 적용
renderTable()            // 테이블 렌더링
sortTable(columnIndex)   // 컬럼 정렬
showModelDetail(code)    // 모달 팝업
createCharts(stats)      // 차트 생성
switchTab(tabName)       // 탭 전환
```

---

## 🚀 배포 프로세스

### 개발 환경
```bash
# 1. 데이터 처리 (Python)
python3 process_data.py \
  --input 판매재고현황_20260915.xlsx \
  --dispose 소진모델.xlsx \
  --output dashboard_data.json

# 2. HTML 생성
python3 generate_html.py \
  --template 호남지사_재고대시보드.html \
  --data dashboard_data.json

# 3. 로컬 테스트
open 호남지사_재고대시보드.html
```

### 배포
```bash
# Artifact 업데이트
claude code artifact update \
  --url https://claude.ai/artifact/... \
  --file 호남지사_재고대시보드.html
```

---

## 📊 기술 스택

### 현재 사용 중
- **Frontend**: HTML5 + Vanilla JavaScript (의존성 없음)
- **Styling**: CSS3 (그리드, 플렉스박스, 미디어 쿼리)
- **Chart**: Chart.js 3.9.1 (CDN)
- **Data**: JSON (HTML 임베드)
- **Processing**: Python 3 (pandas, openpyxl)

### 추천 (향후 마이그레이션)
- **Framework**: React/Vue 3 (컴포넌트 기반)
- **Build**: Vite/Webpack
- **State**: Redux/Pinia
- **Charting**: Recharts/ECharts
- **Database**: PostgreSQL/MongoDB

---

## 🧪 테스트 체크리스트

### 기능 테스트
- [ ] 검색 기능 (모델명, 상품코드)
- [ ] 필터 기능 (제품군, 진열상태, 지점)
- [ ] 정렬 기능 (모든 컬럼)
- [ ] 모달 팝업 (모델 상세)
- [ ] 차트 렌더링

### 성능 테스트
- [ ] 초기 로딩 시간 (< 3초)
- [ ] 필터 반응 속도 (즉시)
- [ ] 정렬 속도 (< 500ms)
- [ ] 메모리 사용량 (< 50MB)

### 호환성 테스트
- [ ] Chrome (최신)
- [ ] Firefox (최신)
- [ ] Safari (최신)
- [ ] Edge (최신)
- [ ] 모바일 화면 (768px 이하)

---

## 🎓 참고 자료

### 데이터 정의
```
지점: 호남지사 내 70+ 하이마트 지점
상품명: LG 제조 제품 (TV, 냉장고, 세탁기 등)
상품코드: LG 모델 코드 (예: OLED77B6XNA.AKRG)
잔여재고: 현재 지점에 있는 대수
당월판매: 해당 달 판매량
회전율: 판매량/재고량 (0.0 ~ 1.0+)
진열상태: 진열대상 | 소진대상 (61개 모델만 정의됨)
```

### 비즈니스 규칙
- ✅ 호남지사만 표시 (혼매호남팀 담당 지역)
- ✅ 재고 0인 항목 제외 (관리 불필요)
- ✅ 소진대상: 진열 중단 예정 모델
- ✅ 제품군: 상품명 키워드로 자동 분류

---

## 📞 연락처 & 에스컬레이션

### 기술 문제
- HTML/JavaScript: Claude 코드 검토 필요
- Python 데이터 처리: 데이터 구조 확인 필요
- 차트 렌더링: Chart.js 문서 참고

### 비즈니스 문제
- 새 제품군 추가: 분류 로직 수정
- 다른 팀 확장: 데이터 필터링 규칙 변경
- 성능 문제: 아키텍처 검토 필요

---

## 📝 추가 노트

### 알려진 이슈
- ⚠️ 대량 데이터 정렬 시 약간의 지연 (가상 스크롤로 해결 가능)
- ⚠️ IE11 미지원 (필요시 바벨 트랜스파일 필요)
- ⚠️ 오프라인 모드 미지원

### 최적화 기회
- 🎯 데이터 압축 (GZIP)
- 🎯 가상 스크롤 (대량 행)
- 🎯 웹 워커 (필터링 병렬 처리)
- 🎯 서비스 워커 (캐싱)

### 향후 고려사항
- 📱 PWA 변환
- 🔐 사용자 인증
- 📊 실시간 데이터 소켓
- 🌐 다국어 지원

